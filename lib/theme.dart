import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const Color background = Color(0xFFE8EDF2);
  static const Color primary = Color(0xFF2C3947);
  static const Color accent = Color(0xFF547A45);
}
